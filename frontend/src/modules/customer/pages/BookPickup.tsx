import {useState} from "react";const steps=["Select Plan","Coverage Area","Carrier","Pickup Details","Date & Time","Add-ons","Preview","Payment"];export function BookPickup(){const[s,setS]=useState(0);return <section className="mx-auto max-w-5xl"><h1 className="text-3xl font-bold text-brand-blue">Book a Pickup</h1><div className="mt-6 flex gap-2 overflow-auto">{steps.map((x,i)=><span className={`whitespace-nowrap rounded-full px-4 py-2 text-sm font-semibold ${i===s?"bg-brand-orange text-white":i<s?"bg-brand-blue text-white":"bg-slate-200"}`} key={x}>{i+1}. {x}</span>)}</div><div className="card mt-6 min-h-96 p-8"><h2 className="text-2xl font-bold">{steps[s]}</h2><p className="mt-2 text-slate-500">Each step will be API-backed in the next implementation phase.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{s===0&&["Basic Pickup — $10","Standard Pickup — $20","Premium Pickup — $35"].map(x=><button className="rounded-2xl border p-5 text-left hover:border-brand-orange" key={x}>{x}</button>)}{s===2&&["Carrier A","Carrier B","Carrier C"].map(x=><button className="rounded-2xl border p-5 text-left hover:border-brand-blue" key={x}>{x}</button>)}{s>=1&&s!==2&&<><input className="input" placeholder="Business / contact name"/><input className="input" placeholder="Pickup address"/><input className="input" placeholder="Phone"/><input className="input" placeholder="Special instructions"/></>}</div><div className="mt-8 flex justify-between"><button disabled={!s} onClick={()=>setS(s-1)} className="rounded-xl border px-5 py-3 disabled:opacity-30">Back</button><button onClick={()=>setS(Math.min(s+1,steps.length-1))} className="btn-primary">{s===steps.length-1?"Book Pickup":"Continue"}</button></div></div></section>}