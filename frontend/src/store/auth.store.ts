import {create} from "zustand"; import type {User} from "../types"; type S={user:User|null;token:string|null;setAuth:(u:User,t:string)=>void;logout:()=>void}; export const useAuthStore=create<S>(set=>({user:null,token:localStorage.getItem("pickup_access_token"),setAuth:(user,token)=>{localStorage.setItem("pickup_access_token",token);set({user,token})},logout:()=>{localStorage.removeItem("pickup_access_token");set({user:null,token:null})}}));