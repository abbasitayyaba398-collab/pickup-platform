# Pickup Platform Starter

React + TypeScript + Vite + Tailwind CSS + Node/Express + MongoDB.

## Run
Frontend:
cd frontend
npm install
cp .env.example .env
npm run dev

Backend:
cd backend
npm install
cp .env.example .env
npm run dev

MongoDB: mongodb://127.0.0.1:27017/pickup_platform
